# Maintenance

Useful maintenance tasks:

- Check dependency compatibility
- Refresh the dataset when new movie metadata is needed
- Validate IMDb scraping after layout changes
- Keep setup instructions aligned with the code
- Remove local cache files before committing
