# Sentiment Analysis

IMDb reviews are collected from the selected movie page and passed through a trained classifier.

The app labels each review as:

- Good
- Bad

The sentiment model is loaded once when Flask starts.
