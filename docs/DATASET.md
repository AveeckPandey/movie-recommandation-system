# Dataset Notes

The recommender reads movie metadata from `main_data.csv`.

The most important columns are:

- `movie_title`
- `comb`

The `comb` column contains combined movie metadata used to calculate content similarity.
